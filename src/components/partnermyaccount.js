import React from 'react'
import db, {firebase} from "../firebase";

export default function Partnermyaccount() {
    let user = firebase.auth().currentUser;


    return (
        <div>
            
        </div>
    )
}
